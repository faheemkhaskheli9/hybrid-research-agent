# Copy to .env and fill in

# Required only when configs/*.yaml sets embedding.provider: openai (see
# src/kb/config.py) -- the default `provider: hashing` needs no API key.
OPENAI_API_KEY=

DATABASE_URL=
